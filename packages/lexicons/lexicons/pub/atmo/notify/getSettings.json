{
  "lexicon": 1,
  "id": "pub.atmo.notify.getSettings",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get the authenticated user's notification settings. User-authenticated.",
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["notifyPendingViaTelegram", "autoAllow", "pushDevices"],
          "properties": {
            "notifyPendingViaTelegram": { "type": "boolean" },
            "autoAllow": {
              "type": "string",
              "enum": ["all", "trusted", "none"],
              "description": "Policy for incoming permission requests: auto-grant all, only trusted apps, or none."
            },
            "pushDevices": {
              "type": "integer",
              "description": "Number of web push subscriptions registered for this account."
            }
          }
        }
      }
    }
  }
}
